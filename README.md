""""
считывания данных с клавиатуры
обработка ошибок:
  проверка на то что было веденно целое число минут и часов
  проверка является ли число минут чисом от 0 до 59
  проверка является ли число часов чисом от 0 до 23
проверка особых случаев когда ответ состоит из одного слова(полночь, полдень)
если запрос не особый случай то:
  обращаемся к индексу tupla с закэшироваными эквивалентными представлениями часов в виде строки
  обрабатываем минуты:
    если число минут == 0 то эквивалентное представление в виде строки ровно
    если число минут == 1 то эквивалентная представление в виде строки 1 минута
    если число минут <= 4, но >= 2 то эквивалентная представление в виде строки число минут минуты
    если число минут <= 59, но >= 5 то эквивалентная представление в виде строки число минут минут
  обработка времени суток:
    если число часов <= 5, но >= 0 то время суток ночь
    если число часов <= 11, но >= 6 то время суток утро
    если число чвсов <= 18, но >= 12 то время суток день
    если число чвсов <= 23, но >= 19 то время суток вечер
  вывод данных с помощью функции обертки printf
  конец програмы
  """
    
  
